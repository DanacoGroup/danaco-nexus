# Sekrety i konfiguracja lokalna
.env
*.env.local

# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Node / frontend
node_modules/
frontend/dist/

# Dane uruchomieniowe i programy instalowane w katalogu projektu
dane/
data/
logs/
programy/
.cache/
# Wzorzec bez ukośnika dopasowuje **każdy** katalog o tej nazwie, na dowolnej głębokości.
# `dane/` złapało przez to `frontend/src/dane/` — katalog z kodem, nie z danymi: leży w nim
# katalog narzędzi (generowany) i ręcznie pisane `zastosowania.ts`, które przez to nigdy
# nie weszło do repozytorium. Katalogi danych uruchomieniowych (`dane/`, `backend/dane/`)
# mają zostać pominięte, więc zamiast przepinać wzorzec na `/dane/` odwracamy wyjątkiem.
!frontend/src/dane/

# Logi budowy i uruchomień lokalnych
.logs/
.tmp/
*.egg-info/

# Robocze drzewa agentów Claude Code
.claude/

# Pakiety marki przeniesione do aplikacji przez frontend/scripts/zasoby.py
frontend/public/kroje/
frontend/public/icons/
# Odznaka powiadomień jest potrzebna service workerowi także bez uruchomienia zasoby.py
!frontend/public/icons/badge-96.png
frontend/public/znak/
frontend/public/tla/
frontend/public/film/
frontend/public/ruch/
frontend/public/ladowanie/
# Ustawienia ekranu ładowania są nasze, nie z pakietu marki: `opcje.js` pisaliśmy tutaj
# i bez niego otwarcie wraca do domyślnego progu, czyli znika na gotowej stronie.
!frontend/public/ladowanie/opcje.js
frontend/public/favicon.ico
frontend/public/favicon.svg
frontend/public/apple-touch-icon.png
frontend/src/tokens.css

# Kopie z telefonu omyłkowo wypakowane do katalogu projektu (SMS, połączenia, ustawienia aplikacji)
design-system/Apps/

# Plik konfiguracji NSS zostawiany przez przeglądarkę uruchamianą w testach
backend/pkcs11.txt

# Wydania: artefakty budowy i dane przedsionka (poza repozytorium).
wydania/
# Ten sam wzorzec bez ukośnika złapał `deploy/wydania/` — czyli **całą obsługę wdrożenia**
# (`zbuduj.sh`, `wypchnij.sh`, `cofnij.sh`, `sprzataj.sh`, `wersje.sh`, README). Skrypty,
# którymi produkt się buduje i wypuszcza, były przez to poza kontrolą wersji: świeży klon
# nie miał czym zbudować wydania. Pliki `.env` zostają pominięte — w nich są ścieżki
# i miejsca na klucze, a te do repozytorium nie należą.
!deploy/wydania/
deploy/wydania/*.env

# Pakiety źródłowe marki i materiałów (ok. 1,9 GB renderów, nagrań i plików roboczych).
# W repozytorium leży gotowy `frontend/public`, który buduje interfejs; pakiety źródłowe
# zostają na serwerze i wchodzą do kopii zapasowej (deploy/kopia-zapasowa.sh).
# Odświeżenie `frontend/public` z pakietów: frontend/scripts/zasoby.py.
/branding/
/design-system/
/landing/
/logo/
/motion/
/prezentacja/
/product/
/promocja/
/ui-kit/
# To samo dotyczy ciężkich katalogów w `frontend/public`: film, kampania i pakiet ruchu
# to pół gigabajta wideo. Budowa bierze je z dysku, kopia zapasowa je zabezpiecza.
/frontend/public/film/
/frontend/public/kampania/
/frontend/public/ruch/
